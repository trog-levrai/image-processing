# include "tools.hh"

namespace tools
{
    std::vector<unsigned char> resize(const std::vector<unsigned char>& arg, size_t w, size_t h)
    {
        std::vector<unsigned char> out(25*25, 0);
        /*int ratio = w / 25;
        
        for(int i = 0; i < 25; ++i)
          for (int j = 0; j < 25; ++j)
          {

          }*/
        return out;
    }
}
